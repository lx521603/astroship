---
import Container from "@/components/container.astro";
import Layout from "@/layouts/Layout.astro";
---

<Layout title="页面未找到 - 404">
  <Container>
    <div class="min-h-[calc(100vh-16rem)] flex flex-col items-center justify-center py-12 px-4">
      <div class="max-w-2xl w-full text-center">
        <!-- 标题区域 -->
        <div class="mb-8">
          <h1 class="text-5xl lg:text-7xl font-bold text-gray-900 mb-4">404</h1>
          <p class="text-xl lg:text-2xl text-gray-600 mb-2">哎呀！页面迷路了</p>
          <p class="text-lg text-gray-500">我们找不到您要查找的页面。</p>
        </div>

        <!-- GitHub风格动画容器 -->
        <div class="relative mb-8 mx-auto max-w-lg">
          <!-- 浮动猫容器 -->
          <div class="relative h-48 mb-8">
            <!-- 猫耳朵 -->
            <div class="absolute top-0 left-1/2 transform -translate-x-1/2 -translate-y-1 z-10">
              <div class="flex space-x-1">
                <div class="w-4 h-4 bg-gray-800 rounded-full"></div>
                <div class="w-4 h-4 bg-gray-800 rounded-full"></div>
              </div>
            </div>
            
            <!-- 猫脸 -->
            <div class="absolute top-4 left-1/2 transform -translate-x-1/2 w-24 h-20 bg-gray-800 rounded-b-full rounded-t-lg">
              <!-- 猫眼睛 -->
              <div class="absolute top-6 left-4 right-4 flex justify-between">
                <div class="w-6 h-6 bg-white rounded-full flex items-center justify-center">
                  <div class="w-3 h-3 bg-gray-900 rounded-full animate-pulse"></div>
                </div>
                <div class="w-6 h-6 bg-white rounded-full flex items-center justify-center">
                  <div class="w-3 h-3 bg-gray-900 rounded-full animate-pulse"></div>
                </div>
              </div>
              <!-- 猫胡子 -->
              <div class="absolute top-14 left-2 right-2 flex justify-between">
                <div class="w-8 h-0.5 bg-white transform -rotate-15"></div>
                <div class="w-8 h-0.5 bg-white transform rotate-15"></div>
              </div>
            </div>

            <!-- 浮动线条（代表GitHub的浮动元素） -->
            <div class="absolute inset-0 overflow-hidden">
              <div class="absolute w-1 h-12 bg-gray-300 rounded top-8 left-1/4 animate-float" style="animation-delay: 0s;"></div>
              <div class="absolute w-1 h-8 bg-gray-300 rounded top-16 left-2/4 animate-float" style="animation-delay: 1s;"></div>
              <div class="absolute w-1 h-10 bg-gray-300 rounded top-12 right-1/4 animate-float" style="animation-delay: 2s;"></div>
              <div class="absolute w-1 h-6 bg-gray-300 rounded top-20 left-1/3 animate-float" style="animation-delay: 0.5s;"></div>
              <div class="absolute w-1 h-14 bg-gray-300 rounded top-10 right-1/3 animate-float" style="animation-delay: 1.5s;"></div>
            </div>
          </div>

          <!-- 代码窗口（GitHub风格） -->
          <div class="bg-gray-900 rounded-lg p-4 text-left mb-6">
            <div class="flex space-x-2 mb-3">
              <div class="w-3 h-3 bg-red-500 rounded-full"></div>
              <div class="w-3 h-3 bg-yellow-500 rounded-full"></div>
              <div class="w-3 h-3 bg-green-500 rounded-full"></div>
            </div>
            <pre class="text-green-400 text-sm overflow-x-auto"><code>// 页面未找到
<span class="text-blue-400">if</span> (!page.exists()) {
  <span class="text-yellow-400">return</span> <span class="text-red-400">new</span> Response(<span class="text-gray-400">"404 - 页面不存在"</span>);
}</code></pre>
          </div>
        </div>

        <!-- 操作按钮 -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
          <a href="/" class="px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition duration-200 font-medium">
            返回首页
          </a>
          <a href="javascript:history.back()" class="px-6 py-3 bg-gray-200 hover:bg-gray-300 text-gray-800 rounded-lg transition duration-200 font-medium">
            返回上页
          </a>
        </div>

        <!-- 辅助文字 -->
        <div class="mt-8 text-sm text-gray-500">
          <p>如果您认为这是一个错误，请联系网站管理员</p>
        </div>
      </div>
    </div>

    <style>
      /* 浮动动画 */
      @keyframes float {
        0%, 100% { transform: translateY(0) rotate(0deg); }
        33% { transform: translateY(-10px) rotate(3deg); }
        66% { transform: translateY(-5px) rotate(-3deg); }
      }
      .animate-float {
        animation: float 3s ease-in-out infinite;
      }
    </style>
  </Container>
</Layout>
